<?xml version="1.0" encoding="UTF-8"?>
<root>
  <layout extension=".view.tsx" suffix="View">import React, { Component } from 'react';

interface Props { }

interface State { }

class ${fileName} extends Component${(}Props,State${)} {

  constructor(props: Props) {
    super(props);
  }

  render() {
    return (
      ${(}${)}

      ${(}/${)}
    );
  }

}

export default ${fileName};</layout>

  <success>component was create</success>
</root>