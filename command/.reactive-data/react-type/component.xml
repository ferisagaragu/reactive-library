<?xml version="1.0" encoding="UTF-8"?>
<root>
  <layout extension=".component.tsx" suffix="Component" folder="true">import React, { Component } from 'react';

interface Props { }

interface State { }

class ${name} extends Component${(}Props,State${)} {

  constructor(props: Props) {
    super(props);
  }

  render() {
    return (
      ${(}${)}
        ${name} rendered
      ${(}/${)}
    );
  }

}

export default ${name};</layout>

  <success>component was create</success>
</root>